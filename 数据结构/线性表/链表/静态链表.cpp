#include <stdio.h>
#include <stdlib.h>

#define MaxSize 10
typedef int ElemType;
typedef struct Node{
	ElemType data;
	int next;
}SLinkList[MaxSize]; //==typedef struct Node SLinkList[MaxSize]





int main (){
	SLinkList a;//==struct Node a[MaxSize]
}
